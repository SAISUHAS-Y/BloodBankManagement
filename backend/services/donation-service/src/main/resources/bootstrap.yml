spring:
  application:
    name: donation-service
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888}
      fail-fast: true
      retry:
        initial-interval: 3000
        multiplier: 1.5
        max-attempts: 6
